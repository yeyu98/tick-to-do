import type { ThemeConfig } from 'antd'

const Layout = {
  lightSiderBg: '#e6f4ff',
  headerBg: '#e6f4ff',
}

const Menu = {
  itemBg: '#e6f4ff',
  itemSelectedBg: '#d4e6fe',
}

const theme: ThemeConfig = {
  components: {
    Layout,
    Menu,
  },
}

export default theme
